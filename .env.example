AWS_BEARER_TOKEN_BEDROCK=
AWS_REGION=us-west-2

# The HTTP Basic password a deployed copy sits behind, under the user name
# "orla". `just deploy` refuses to run without one and puts it in Secrets
# Manager. A local run never reads it, so `just serve` still asks for nothing.
DEPLOY_PASSWORD=
